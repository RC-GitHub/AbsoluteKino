services:
  api:
    image: absolutekino-prod:latest  
    build:
      context: .
      dockerfile: Dockerfile   
    ports:
      - "${EXTERNAL_PORT:-3001}:${APP_PORT:-3000}"
    env_file:
      - .env     
    environment:
      - NODE_ENV=prod
    restart: always          